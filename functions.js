function greet(Bob) {
    console.log("Hello, " + Bob + "!")
}

greet("Bob");

let diameter = 10;
if(diameter > 0) {
    let pi = 3.14;
    let circumference = pi * diameter;
    console.log("The circumference is: " + circumference);
}